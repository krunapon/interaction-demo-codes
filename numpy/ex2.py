import numpy as np
# a = np.arange(1,6).reshape((1,5))
# firstRow = 2*a
# secondRow = 10 + firstRow
# thirdRow = 20 + firstRow
# fourthRow = 30 + firstRow
# result = np.row_stack((firstRow, secondRow, thirdRow, fourthRow))
# print(result)
b = 2 + 2*np.arange(20).reshape((4,5))
print(b)